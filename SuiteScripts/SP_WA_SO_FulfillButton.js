/**
 * @NApiVersion         2.1
 * @NScriptType         WorkflowActionScript
 */

define(
    [],
    () => {
        const onAction = (context) => {
            let title = 'WA|SO Fulfill Button';
            
        };

        return {
            onAction
        };
    }
);